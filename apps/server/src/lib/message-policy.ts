/** Recall (withdraw) window — aligned with common IM apps (WeChat ~2 min). */
export const MESSAGE_RECALL_WINDOW_MS = 3 * 60 * 1000;

/** Edit window — longer than recall; edits are not broadcast in real time. */
export const MESSAGE_EDIT_WINDOW_MS = 15 * 60 * 1000;
